{% extends 'base.html.twig' %}

{% block title %}Car !{% endblock %}

{% block body %}
{# <br> #}

<div class="row justify-content-center m-4">
<div class="col w-75 bg-warning rounded p-4" style="display: flex; flex-wrap: wrap;">
<div class="row justify-content-center m-3">
{# <hr> #}
<div class="card text-white bg-dark m-5 w-75">
  <h1 class="card-header">
  <a style="text-decoration: none;" href="{{path('product', {'id' : product.id})}}"><div class="row rounded justify-content-center" style="color: orange;">{{product.name}}</div></a>
</h1>
  <div class="card-body">
    <h3 class="card-title">
    <p>{{product.subtitle}}</p>
</h3>
  </div>

    <div class="row justify-content-center mx-auto">
    <a href="{{path('product', {'id' : product.id})}}"> <img alt="oups" class="row img-fluid rounded justify-content-center m-1" src="/assets/img/products/images/{{product.image}}"></a>
</div>

  <div class="card-body">
    <p class="card-text"><p>{{product.description}}</p>
  </div>
  <div class="card-body">
    <p>{{ (product.price /100) |number_format(2,',','.')}} € </p>
  </div>
  <div class="card-body">
    <a href="{{ path('ajouterPanier', { 'id' : product.id})}}" class="btn btn-info">Add to cart</a>
  </div>
  </div>
<hr>
  </div>
  </div>
  </div>


{# <div class="row">
<div class="col-sm-9">
<div class="row">
<img alt="oups" class="w-25 h-75 m-5" src="/assets/img/products/images/{{product.image}}">
<span>{{product.name}}  </span>
<p>{{ (product.price /100) |number_format(2,',','.')}} € </p>
<p>{{product.subtitle}}</p>
<p>{{product.description}}</p>
<a href="{{ path('ajouterPanier', { 'id' : product.id})}}" class="btn btn-primary w-25">Add to cart</a>


</div>
</div>
</div> #}

{% endblock %}
