{% extends 'base.html.twig' %}

{% block title %}Catalogue{% endblock %}

{% block body %}
{# <div class="row">
<div class="col-sm-9">
<div class="row"> #}

{# <div class="col-md-4"> #}
<div class="row">
<div class="col-sm-9">
<div class="row">
<img alt="oups" class="w-25 h-75 m-5" src="/assets/img/products/images/{{product.image}}">
<span>{{product.name}}  </span>
<p>{{ (product.price /100) |number_format(2,',','.')}} € </p>
<p>{{product.subtitle}}</p>
<p>{{product.description}}</p>
<a href="{{ path('ajouterPanier', { 'id' : product.id})}}" class="btn btn-primary w-25">Ajouter au panier</a>


</div>
</div>
</div>
{# </div> #}

{# 
</div>
</div>
</div> #}




{% endblock %}
