{% extends 'base.html.twig' %}

{% block title %}Car !{% endblock %}

{% block body %}
<br>

<div class="row justify-content-center align-items-center">
    <div class="col-4 col-5 col-6 col-7 col-8 col-9 col-10">
<hr>
<div class="card text-white bg-primary m-5 w-75">
  <h3 class="card-header">
  <a href="{{path('product', {'id' : product.id})}}"><div style="color: orange;">{{product.name}}</div></a>
</h3>
  <div class="card-body">
    <h5 class="card-title">
    <p>{{product.subtitle}}</p>
</h5>
  </div>
    <div>
    <a href="{{path('product', {'id' : product.id})}}"> <img alt="oups" class="w-50 h-50 m-5" src="/assets/img/products/images/{{product.image}}"></a>
</div>
  <div class="card-body">
    <p class="card-text"><p>{{product.description}}</p>
  </div>
  <div class="card-body">
    <p>{{ (product.price /100) |number_format(2,',','.')}} € </p>
  </div>
  <div class="card-body">
    <a href="{{ path('ajouterPanier', { 'id' : product.id})}}" class="btn btn-warning">Add to cart</a>
  </div>
  </div>
<hr>
  </div>
  </div>


{# <div class="row">
<div class="col-sm-9">
<div class="row">
<img alt="oups" class="w-25 h-75 m-5" src="/assets/img/products/images/{{product.image}}">
<span>{{product.name}}  </span>
<p>{{ (product.price /100) |number_format(2,',','.')}} € </p>
<p>{{product.subtitle}}</p>
<p>{{product.description}}</p>
<a href="{{ path('ajouterPanier', { 'id' : product.id})}}" class="btn btn-primary w-25">Add to cart</a>


</div>
</div>
</div> #}

{% endblock %}
